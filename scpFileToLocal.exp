#!/usr/bin/expect
#scpFileToLocal.exp

set file [lindex $argv 0]
set host [lindex $argv 1]
set passwd [lindex $argv 2]

spawn scp -r root@$host:$file /home/mysql/syncdata
expect {
  "(yes/no)?" {send "yes\n"; exp_continue;}
  "*assword:" {send "$passwd\n";}
}

expect "100%"
expect eof



